<tr>
           	<td>
           		<%= link_to product_path(cart_item.product) do %>
                    <% if cart_item.product.image.present? %>
                    	<%= image_tag(cart_item.product.image.thumb.url, class: "thumbnail") %>
                    	<% else %>
                    	 <%= image_tag("http://placehold.it/200x200&text=No Pic", class: "thumbnail") %>
                    <% end %>
           		<% end %>
           	</td>
           	<td><%= link_to(cart_item.product.title, product_path(cart_item.product)) %></td>
           	<td><%= cart_item.product.price %></td>
           	<td >
                <input type="text" name="quantities[]" value="<%= cart_item.quantity %>" class="cart-item-quantity-<%= cart_item.id %> disable" >
           		<%= link_to decrease_cart_item_path(cart_item.product.id), :remote => true, method: :post, class: "btn btn-round btn-info btn-xs"  do%>
           		<i class="material-icons">remove</i>
           		<% end %>
           		<%= link_to increase_cart_item_path(cart_item.product.id), :remote => true, method: :post, class: "btn btn-round btn-info btn-xs" do %>
           		<i class="material-icons">add</i>
           		<% end %>	
           		</td>
           	<td class="cart-item-price-<%= cart_item.id %>">
           		<%= cart_item.quantity * cart_item.product.price %>           			
           		</td>
           	<td><%= link_to("删除", cart_item_path(cart_item.product_id), :method => :delete) %></td>
           </tr>	